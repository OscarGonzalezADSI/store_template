function controller_form_contact(nodes)
{
	moduleContactTitle(nodes);
	moduleContactName(nodes);
	moduleContactEmail(nodes);
	moduleContactSubject(nodes);
	moduleContactMessage(nodes);
	moduleContactSubmit(nodes);
	moduleContactAppend(nodes);
}